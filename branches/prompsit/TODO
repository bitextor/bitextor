* Evaluate if it would be interesting to remove words appearing in too many documents in idx file
* Check if LangID does not have problems when detecting language for HTML -> change the module in which text is cleaned for optimisation
